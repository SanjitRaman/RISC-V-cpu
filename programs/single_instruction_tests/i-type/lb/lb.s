lb a0, 0(zero) # Load the 1st byte in memory to a0
lb a0, 1(zero) # Load the 2nd byte in memory to a0
lb a0, 2(zero) # Load the 3rd byte in memory to a0
lb a0, 3(zero) # Load the 4th byte in memory to a0
lb a0, 4(zero) # Load the 5th byte in memory to a0
lb a0, 5(zero) # Load the 6th byte in memory to a0
lb a0, 6(zero) # Load the 7th byte in memory to a0
lb a0, 7(zero) # Load the 8th byte in memory to a0